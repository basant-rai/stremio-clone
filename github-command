# command line 
# Initialize
git init (initialize)

# Add files
git add .

# Commit/comment
git commit -m "write commit description"

# pull 
git pull

# push 
git push -u origin <branch name>

# clone
git clone <git url>

#create new branch
git branch <branch-name>

# create an switch to new branch
git checkout -b <branch name>

#To switch to new branch
git checkout <branch name>

# to see the branch list
git branch
